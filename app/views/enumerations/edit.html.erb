<%= t @enumeration.option_name %>

<%=form_for :enumeration, object: @enumeration, :url => enumeration_path(@enumeration), html: {method: 'put', class: 'form-horizontal' } do |f| %>
    <%= f.hidden_field :type  %>
    <%= render :partial => 'form', :locals => {:f => f} %>
    <div class="form-group" >
      <div class="col-md-11">
        <%= f.submit t(:button_save), class: 'btn btn-primary pull-right' %>
      </div>
    </div>
<% end %>
