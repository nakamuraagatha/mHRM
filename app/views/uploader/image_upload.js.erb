<% if current_user.errors.any? %>
alert('<%= current_user.errors.full_messages.join(', ') %>');
<% else %>
$('.files').html("");
$('.files').html("<%= escape_javascript(render partial: 'uploader/display_image') %>");
<% end %>