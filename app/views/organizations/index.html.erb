<h1><%= t(:organizations) %></h1>

<%= link_to( t(:new_organization), new_organization_path, class: 'btn btn-primary pull-right') if can?(:create_organizations, :manage_organizations, :manage_roles) %>
<%= render partial: 'list', locals:{organizations: @organizations}  %>